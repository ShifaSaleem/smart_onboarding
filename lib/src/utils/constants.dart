// constants.dart

import 'package:flutter/material.dart';

// Colors
const Color kPrimaryColor = Color(0xFF6C63FF);
const Color kSecondaryColor = Color(0xFFF5F5F5);
const Color kAccentColor = Color(0xFF4CAF50);
const Color kTextColor = Colors.black87;
const Color kTitleColor = Color(0xFF333333);

// Padding
const EdgeInsets kDefaultPadding = EdgeInsets.symmetric(horizontal: 20.0);
const double kDefaultSpacing = 16.0;
const double kButtonRadius = 12.0;

// Durations
const Duration kDefaultAnimationDuration = Duration(milliseconds: 300);

// Curves
const Curve kDefaultAnimationCurve = Curves.easeInOut;

// Fonts
const String kFontFamily = 'Roboto';
